# Database Configuration
DATABASE_URL="postgresql://username:password@host:port/database_name?schema=public"

# JWT Configuration (Generate secure secrets for production!)
JWT_ACCESS_SECRET="your_secure_access_secret_here"
JWT_REFRESH_SECRET="your_secure_refresh_secret_here"

# Server Configuration
PORT=10000
NODE_ENV=production

# CORS Configuration
CORS_ORIGIN="https://your-frontend-domain.com"

# Upload Configuration
UPLOAD_DIR=uploads

# For Render.com deployment:
# 1. Create PostgreSQL database service in Render
# 2. Copy the Internal Database URL to DATABASE_URL
# 3. Generate secure JWT secrets (use strong random strings)
# 4. Set CORS_ORIGIN to your frontend deployment URL
